/**
 * @param {Function} fn
 * @param {Array} args
 * @param {number} t
 * @return {Function}
 */
var cancellable = function(fn, args, t) {
    let c = setTimeout(() => fn(...args), t);
    return () => clearTimeout(c);
};
